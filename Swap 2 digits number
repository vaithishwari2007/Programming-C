#include <stdio.h>

int main() {

    int num;

    scanf("%d", &num);



    if (num < 10) {

        printf("STDOUT\n");

        return 0;

    }

    int temp = num;

    while (temp >= 100) {

        temp /= 10;

    }

    int first = temp / 10;

    int second = temp % 10;

    printf("%d%d\n", second, first);

    return 0;

}
